---
applyTo: "**/+page.svelte
---

If using a card in a front-end page, use the Card component in lib/components/common/card/Card.svelte instead of creating a new div with card classes. This ensures a consistent design and makes it easier to maintain the code.
