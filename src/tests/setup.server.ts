// Setup for the `server` Vitest project (node): services, database, API routes, notifications,
// emails — everything that is NOT a `*.svelte.test.ts` component suite.
//
// Intentionally minimal for now. Kept for symmetry with setup.client.ts and as the home for any
// future node-environment hooks (global fetch stubs, time control, etc.).
export {};
